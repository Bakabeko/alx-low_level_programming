#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
char *_strchr(char *s, char c)
{
	while (s[i] != '\0')
	{
		if (s[i] == c)
		{
			return ((s+i));
		}
	}
	if (c == NULL)
		return ((s+i);
        return NULL;
}
